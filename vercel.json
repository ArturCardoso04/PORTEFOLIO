{
  "installCommand": "cd portfolio && npm install",
  "buildCommand": "cd portfolio && npm run build --workspace frontend",
  "outputDirectory": "portfolio/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
